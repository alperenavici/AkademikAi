@{
    ViewData["Title"] = "Home Page";
}


<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AkademikAi - Hızlı Öğren, Motive Kal, Akıllı Çalış</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="~/css/active.css" rel="stylesheet" />

    <style>
        /* --- TEMEL STİLLER VE RESETLEME --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f8f8;
            color: #1f2937;
            overflow-x: hidden;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* --- HEADER / ÜST BİLGİ --- */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem 0;
        }

            .header .logo {
                font-size: 1.75rem;
                font-weight: 700;
            }

            .header .nav-links {
                display: flex;
                gap: 0.5rem;
            }

                .header .nav-links a {
                    padding: 0.5rem 1rem;
                    border-radius: 8px;
                    font-size: 0.9rem;
                    font-weight: 500;
                    transition: background-color 0.2s;
                }

                    .header .nav-links a:hover {
                        background-color: #e5e7eb;
                    }

            .header .user-actions {
                display: flex;
                align-items: center;
                gap: 1.5rem;
                font-size: 0.9rem;
                font-weight: 500;
            }

                

        /* --- HERO / ANA BÖLÜM --- */
        .hero-section {
            display: flex;
            align-items: center;
            gap: 2rem;
            margin-top: 2rem;
            min-height: 60vh;
        }

        .hero-left {
            /* Sol tarafın sağ taraftan biraz daha fazla yer kaplamasını sağlıyoruz. */
            flex: 1.1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .hero-right {
            flex: 0.9;
        }

        /* --- YENİ VE KARARLI YAPI --- */
        .device-stack {
            /* Bu bizim ana konumlandırma konteynerimiz.
                   Tüm görseller ve metinler buna göre hizalanacak. */
            position: relative;
            width: 100%;
            max-width: 800px; /* Laptop görselinin maksimum genişliği */
            /* Görselin en-boy oranını koruyarak küçülmesini sağlar.
                   Bu, kaymayı önleyen en önemli kısımdır. */
            aspect-ratio: 700 / 460;
        }

            .device-stack img {
                /* Görsellerin konteyner dışına taşmasını engeller. */
                position: absolute;
                object-fit: contain;
            }

        .laptop-frame {
            width: 120%;
            height: 120%;
            top: 5%;
            right: 0%;
            z-index: 1; /* En alttaki katman */
        }

        .phone-frame {
            width: 19%; /* Genişliği ana konteynere göre oranlı */
            bottom: 45%;
            right: 10%;
            z-index: 1; 
        }

        .screen-content {
            position: absolute;
            z-index: 2; 
            top: 68%;
            left: 25%;
            transform: translate(-50%, -50%);
            width: 50%;
            text-align: left;
            padding: 0.5rem;
        }

            .screen-content h1 {
                font-size: clamp(1.5rem, 2vw, 4rem); /* Ekran boyutuna göre esnek font boyutu */
                font-weight: 800;
                line-height: 1.2;
                color: #111827;
            }

            .screen-content p {
                font-size: clamp(0.5rem, 1.5vw, 0.85rem); /* Esnek font boyutu */
                color: #4b5563;
                margin: 0.5rem auto 0;
                line-height: 1.5;
                max-width: 100%;
            }

            .screen-content .cta-button {
                margin-top: 0.2rem;
                margin-left:0;
                padding: clamp(0.1rem, 0.5vw, 0.3rem) clamp(0.4rem, 1vw, 1rem);
                background-color: #1d4ed8;
                color: #ffffff;
                border: none;
                border-radius: 5px;
                font-size: clamp(0.7rem, 1.5vw, 0.9rem);
                font-weight: 500;
                cursor: pointer;
                transition: background-color 0.2s;
                transform:scale(0.9);
            }

                .screen-content .cta-button:hover {
                    background-color: #1e40af;
                }

        .hero-right .woman-image {
            width: 150%;
            max-width: 510px;
            height: auto;
            display: block;
            margin-left: auto;
        }

        /* --- RESPONSIVE / MOBİL UYUMLULUK --- */
        @@media (max-width: 992px) {
            .header .nav-links {
                display: none; /* Menüyü gizle */
            }

            .hero-section {
                /* Düzeni dikey (alt alta) yap */
                flex-direction: column;
                text-align: center;
            }

            .hero-right {
                order: -1; /* Kadın görselini en üste al */
                margin-bottom: 2rem;
            }

                .hero-right .woman-image {
                    max-width: 350px;
                    margin: 0 auto;
                }
            /* Mobil görünümde cihaz görsellerini gizle ve metni normal akışa döndür */
            .device-stack {
                position: static;
                aspect-ratio: auto; /* Oranlamayı iptal et */
                max-width: 500px; /* Metin genişliğini sınırla */
                margin: 0 auto;
            }

            .laptop-frame,
            .phone-frame {
                display: none; /* Cihazları gizle */
            }

            .screen-content {
                position: static;
                transform: none; /* Konumlandırmayı sıfırla */
                width: 100%;
                padding: 0;
            }
        }

        @@media (max-width: 576px) {
            .container {
                padding: 0 1rem;
            }

            .header .logo {
                font-size: 1.5rem;
            }

            .screen-content h1 {
                font-size: 2.2rem; /* En küçük ekranlar için başlık boyutu */
            }

            .hero-right .woman-image {
                max-width: 90%; /* Görselin ekranı doldurmasını sağla */
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <!-- HEADER -->
        <header class="header">
            <div class="logo">AkademikAi.com</div>
            <nav class="nav-links">
                <a asp-controller="Home" asp-action="Index" class="active">Ana Sayfa</a>
                <a asp-controller="Home" asp-action="About" >Hakkımızda</a>
                <a asp-controller="Home" asp-action="Contact">İletişim</a>
                <a asp-controller="Home" asp-action="Payment">Ödeme</a>

            </nav>
            <div class="user-actions">
                <a asp-controller="User" asp-action="Login">Log In</a>
                <a asp-controller="User" asp-action="Register">Sign Up</a>
              
            </div>
        </header>

        <!-- HERO SECTION -->
        <main class="hero-section">
            <div class="hero-left">
                <div class="device-stack">
                   
                    <img src="~/images/PHONE.png" alt="Telefon çerçevesi" class="phone-frame">
                    <img src="~/images/Device.png" alt="Laptop çerçevesi" class="laptop-frame">

                    <div class="screen-content">
                        <h1>
                                Hızlı Öğren.<br>
                                Motive Kal<br>
                                Akıllı Çalış
                        </h1>
                        <p>
                        Binlerce sorunun içinde kaybolmak yerine, en çok ihtiyacın olan konulara odaklan.
                        Akademik.ai, performansını analiz ederek sana özel bir çalışma planı sunar ve yapay zeka ile hedeflerine en akıllı yoldan ulaşmanı sağlar. 
                            
                        </p>
                        <button class="cta-button">Detayları Gör</button>
                        
                    </div>
                </div>
            </div>

            <!-- Sağ Taraf: Kadın Görseli -->
            <div class="hero-right">
                <!-- Bu görseli kendi projenizdeki dosyayla değiştirin -->
                <img src="~/images/girl.png" alt="Laptop kullanan gülümseyen kadın" class="woman-image">
            </div>
        </main>
    </div>

</body>
</html>
